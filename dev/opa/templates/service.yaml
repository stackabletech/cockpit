---
apiVersion: v1
kind: Service
metadata:
  name: opa
  namespace: default
spec:
  type: NodePort
  selector:
    app: opa
  ports:
    - name: http
      port: 8181
      targetPort: 8181
      nodePort: { { .Values.nodePort } }
