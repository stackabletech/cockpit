---
apiVersion: v1
kind: Service
metadata:
  name: postgresql
  namespace: default
spec:
  type: NodePort
  selector:
    app: postgresql
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
      nodePort: {{ .Values.nodePorts.postgres }}
