# Disable TLS certificate verification for development (e.g. self-signed Trino certs).
# This file is committed and loaded automatically in dev mode.
# Never set this in production.
NODE_TLS_REJECT_UNAUTHORIZED=0
