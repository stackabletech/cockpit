{{- if not .Values.sessionSecret.secretKeyRef.name }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "cockpit.fullname" . }}
  labels:
    {{- include "cockpit.labels" . | nindent 4 }}
type: Opaque
stringData:
  session-secret: {{ include "cockpit.sessionSecret" . | quote }}
{{- end }}
